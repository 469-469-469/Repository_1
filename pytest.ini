[pytest]
markers =
    smoke: быстрые тесты
    critical: критические тесты
    regression: регрессионные тесты
    slow: медленные тесты
    api: API-тесты
    positive: позитивные тесты
    negative: негативные тесты
    rbac: тесты прав доступа

    user: тесты по работе с аккаунтами
    payment: тесты оплаты
    movies: тесты работы с фильмами

log_cli = 1
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
log_cli_level = INFO
log_cli_format = %(asctime)s %(levelname)s %(message)s
log_cli_date_format=%Y-%m-%d %H:%M:%S

#log_cli_format = "%(asctime)s.%(msecs)03d %(message)s"

